# 汎用Diff 匿名テスト結果

## テスト構成

Excel上で`UniversalDesignDiff.bas`を取り込み、`RunUniversalDiffFromPaths`を実行しました。

- 過去設計書：物理インターフェース3、VLANインターフェース2、AP2台
- 今回設計書：物理インターフェース4、VLANインターフェース2、AP2台
- 変更：AP説明、管理IP、認証方式、VLAN、SVI IP
- 追加：監視カメラ用ポート、物理ポート1本
- 移動：利用者端末行、VLAN行、備考行
- Config：LF改行のCisco形式サンプル
- 秘密情報：username secret、SNMP communityの値を変更

## 初回テストで検出した問題

1. LF改行だけのConfigが1行として読み込まれた。
2. 行挿入後のセルが大量の変更として表示された。

次の修正を実施しました。

- Configをバイナリで読み、CRLF、LF、CRを統一して分割
- 同一シート・同一列・同一値を移動候補として先に対応付け
- 移動に該当しない同一セルだけを変更として判定
- 秘密情報はマスクしたまま、内部署名で値の変更を検出

## 修正版の結果

修正版VBAはMicrosoft Excel上で最後まで実行でき、以下のシートを生成しました。

- `UD_サマリ`
- `UD_比較ルール`
- `UD_実行ログ`

`UD_Excel差分`と`UD_Config差分`は内部データとして生成されますが、通常画面では非表示です。

## FG60形式テスト

提示されたFG60設計書と同じ「ネットワーク」「ルーティング」の書式・結合セルを使い、過去版と今回版を作成してMicrosoft Excel上で比較しました。複数インターフェースを含む528行のネットワークシートと、134行のルーティングシートが対象です。

- 差分合計：9件
- 変更：6件（WAN・管理LANのエイリアス/IP、デフォルトルートのゲートウェイ/距離）
- 追加：1件
- 削除：1件
- 移動候補：1件

結果ブックには、元書式を維持した`UD_旧_ネットワーク`、`UD_旧_ルーティング`、`UD_新_ネットワーク`、`UD_新_ルーティング`も生成されました。変更は黄、追加は青、削除は赤、移動候補は緑で着色されることを確認しました。元のテストブックは更新されません。

## AP-635 シート対応表テスト

過去版の`東京_社員SSID`と今回版の`大阪_業務SSID`で検証しました。対応表が0件でも、両方の1番目のシートとして自動対応され、SSIDと認証方式の変更2件になりました。同じ組み合わせを`UD_シート対応表`へ明示登録した場合も変更2件です。

- 差分表に過去・今回の実シート名を記録
- 対応表なしの左から順番による自動対応を確認
- 名前が異なるシートの同一セルを変更として検出
- 過去版・今回版の色付きコピーで変更セルを黄色表示
- 数式エラーなし
- FG60の同名シート比較は従来と同じ9件を維持

## 出力シート配置テスト

- 左端1番目：`UD_旧_東京_社員SSID`
- 左端2番目：`UD_新_大阪_業務SSID`
- 初期表示：旧版の色付きシート
- サマリ、実行ログ、比較ルール、シート対応表：右側へ配置
- Excel差分、Config差分：VeryHiddenで非表示
- 空の初期シート：非表示

複数インターフェースは、セル単位で変更、追加、削除、移動候補として表示されます。行を途中へ追加した場合、後続行の同一値は移動候補になります。

## 確認済み

- 結合セルは左上セルだけを比較
- 表紙シートは既定ルールで除外
- インターフェース行の追加と移動候補を検出
- AP管理IPと認証方式の変更を検出
- LF改行Configを複数行として比較
- 秘密情報を結果へ平文表示しない
- 実行後にExcelの`AccessVBOM`を元の未設定状態へ復元

## 既知の制限

- 汎用Excel Diffはセル単位であり、インターフェースを1オブジェクトとして解析しません。
- 同一列に同じ値が複数ある場合、別のセルを移動先として対応付ける可能性があります。
- Config行Diffは階層を解析しないため、同じコマンドが別インターフェースへ移動した場合は検出できないことがあります。
- メーカー固有の意味的な判定には、別途メーカー別アダプターが必要です。
