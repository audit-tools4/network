# 汎用 詳細設計書／Config Diff（Excel VBA）

`UniversalDesignDiff.bas`は、FortiGateに限らず、SW、AP、ルータ、Firewallなどの詳細設計書を過去版と今回版で比較するVBAモジュールです。必要に応じて、過去Configと今回Configのテキスト差分も同時に確認できます。

Python、Java、外部ライブラリ、外部通信は使用しません。選択した設計書とConfigは会社PC内で読み取るだけで、元ファイルを更新しません。

## 導入

1. GitHubで`UniversalDesignDiff.bas`を開く。
2. コード全体をコピーする。
3. 空のExcelブックを`.xlsm`で保存する。
4. `Alt + F11`を開く。
5. 「挿入」→「標準モジュール」を選ぶ。
6. コードを貼り付けて保存する。
7. `Alt + F8`から`RunUniversalDiff`を実行する。

## 実行時に選択するファイル

1. 過去の詳細設計書Excel
2. 今回の詳細設計書Excel
3. Configも比較する場合は過去Config
4. 今回Config

ファイル選択を使わない場合は、次の形でも実行できます。

```vb
RunUniversalDiffFromPaths _
    "C:\work\old_design.xlsx", _
    "C:\work\new_design.xlsx", _
    "C:\work\old_config.txt", _
    "C:\work\new_config.txt", _
    True
```

## 生成シート

- `UD_サマリ`：対象ファイルと差分件数
- `UD_Excel差分`：設計書の変更、追加、削除、移動候補
- `UD_Config差分`：Configの追加行、削除行
- `UD_比較ルール`：除外、拠点差分、秘密情報のルール
- `UD_シート対応表`：名前が異なる過去版・今回版シートの組み合わせ
- `UD_実行ログ`：実行記録とエラー
- `UD_旧_*`：過去版の比較対象シートをコピーし、差分セルを着色
- `UD_新_*`：今回版の比較対象シートをコピーし、差分セルを着色

色は、変更・数式変更が黄、追加が青、削除が赤、移動候補が緑です。変更は旧版・今回版の両方、追加は今回版、削除は旧版、移動候補は両方を着色します。結合セルの場合は結合範囲全体を着色します。元の過去版・今回版は変更しません。

## Excel比較

結合セルは結合範囲の左上セルを代表として比較します。全角空白、改行、タブ、連続空白は正規化します。

判定は次のとおりです。

- `変更`：同じシート・セルで値が変更
- `数式変更`：表示値は同じだが数式が変更
- `追加`：今回版だけに存在
- `削除`：過去版だけに存在
- `移動候補`：同じシート内で同じ正規化値が別セルへ移動

`移動候補`は、同じ値が複数存在する場合に別のセルと対応する可能性があります。確定判定ではなく、人間の確認対象です。

## 名前が異なるシートの比較

通常は対応表を空欄のまま使用できます。表紙などの比較除外シートを飛ばし、過去版と今回版の比較対象シートを左から1番目同士、2番目同士という順番で自動対応します。APのSSID別シートなどで順番が異なる場合だけ、実行前に`UD_シート対応表`へ登録します。初回実行時に表が自動作成されます。先に表だけ作る場合は`ResetSheetMappings`を実行してください。

| 有効 | 過去版シート名 | 今回版シート名 | 用途・説明 |
|---|---|---|---|
| ○ | 東京_社員SSID | 大阪_業務SSID | AP-635 社員用SSID |

- 有効列は`○`、`ON`、`1`などを指定できます。
- 対応表の登録を優先し、未登録の残りのシートは左から順番に比較します。
- 対応表が0件でも、シート名が異なる1番目同士を比較できます。
- シートの追加・削除や並べ替えがある場合は、意図しない組み合わせを避けるため対応表を使用してください。
- 対応表では今回版シート名を比較上の基準名として使います。
- 同じ過去版シートまたは今回版シートを複数行へ登録すると、安全のため処理を中断します。
- 差分表の`過去シート`と`今回シート`列に、実際のシート名が記録されます。
- 対応を解除する場合は有効列を空欄または`×`にして再実行します。

## 比較ルール

初回実行時に`UD_比較ルール`が作成されます。上から最初に一致したルールを適用します。

| 有効 | 対象 | シート/ファイル | 項目/文言 | 処理 |
|---|---|---|---|---|
| ○ | Excel | 表紙 | * | 比較除外 |
| ○ | Excel | * | ホスト名 | 拠点差分 |
| ○ | Config | * | password | 秘密情報 |

- `シート/ファイル`では`*`と`?`のワイルドカードが使えます。
- `項目/文言`は部分一致です。
- ルールを無効にする場合は、有効列を空欄または`×`にします。
- ルールを変更した後に再実行してください。
- 初期値へ戻す場合は`ResetUniversalRules`を実行します。

## Config比較

Config比較はメーカー非依存の行集合比較です。

- 空行を除外
- 行頭・行末、タブ、連続空白を正規化
- `#`または`!`で始まるコメント行を除外
- 同一行の出現回数を考慮
- 行の順番だけの変更は差分にしない
- 秘密情報ルールに該当する行は値を`********`へ置換
- CRLF、LF、CRの改行コードに対応
- 秘密情報の値が変わった場合は、値を表示せず追加・削除として検出

行の変更は、原則として「旧行の削除」と「新行の追加」の2件で表示されます。

## 重要な制限

- 記載位置が大幅に変更された場合、汎用比較では正しい項目対応を保証できません。
- 同じ値が多数ある場合、移動先を一意に決められません。
- シート対応表はシート全体を対応させます。1枚のシート内に複数SSIDが混在する場合は、SSID単位の意味的対応までは行いません。
- 順番による自動対応は、両方のブックで比較対象シートの並びが同じ意味であることを前提とします。
- セル書式、図形、画像、チェックボックス、コメントの差分は対象外です。
- 色付きコピーは元シートの書式を保持するため、シート数や画像が多い設計書では結果ブックの容量が増えます。
- 非表示シート・非表示行もUsedRange内であれば比較対象です。
- Config比較はコマンド構造を解釈しません。順序が意味を持つACL、ポリシー、route-mapなどは人間の確認が必要です。
- FortiGateの意味的なConfig照合は、別モジュール`FG60_SelfContained.bas`を使用してください。
- マクロブック自身を比較対象として選択しないでください。
- 差分ゼロは設定の妥当性や完全性を保証しません。

## セキュリティ

- 元Excelは読み取り専用で開き、保存せず閉じます。
- Configは読み取りのみです。
- 外部通信、メール送信、コマンド実行、レジストリ変更を行いません。
- 差分結果には設計値やConfig行が記録されるため、生成した`.xlsm`も機密情報として管理してください。
- 秘密情報ルールは補助機能です。未知のキーワードは自動マスクされないため、本番前にルールを確認してください。

## 本番導入前の確認

匿名化したテストファイルで、少なくとも次を確認してください。

- 同じファイルで差分ゼロになる
- 値を1つ変更すると`変更`になる
- 行を追加すると`追加`または`移動候補`になる
- シートを追加・削除した場合にセルが追加・削除として出る
- Config行の追加・削除を検出する
- password、secret、PSKなどが結果でマスクされる
- 表紙・改訂履歴などの除外ルールが機能する
